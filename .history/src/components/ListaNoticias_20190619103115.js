import React from 'react';

const ListaNoticias = ({noticias}) => 
( 
    <div className="row">
        {noticias.map(noticia => (
            
        ))}
    </div>
);

 
export default ListaNoticias;